## MySQL vs MongoDB Implementation

- MySQL uses normalized tables and JOIN queries
- MongoDB stores order data as embedded documents
- API response structure is identical in both cases
- Only the repository layer differs
